# things in games
# event loop/game loop/ mainloop
# camera
# movable character
#   center = x,y
#   can assign new coordinate to center
#   box.left = x
# import gamebox
# import pygame
# camera = gamebox.Camera(800,600)
#
# colors = ['red', 'green', 'blue']
# frame = 0
# def tick(keys):
#     global frame
#     camera.clear(colors[frame])
#     frame += 1
#     if frame >= len(colors)
#         frame = 0
#     camera.display()
#
# ticks_per_second = 30



import random
help(random)